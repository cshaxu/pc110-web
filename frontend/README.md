# Transitional Frontend Directory

The integrated layout supersedes this directory. Future Next.js application
code belongs in `../src/app/` and may consume only the stable API from
`../src/emulator/runtime/`. Do not add new implementation here.
