# M0 Td S3 P1 Archived Proposal: Web Documentation Scope

The user explicitly required governance documentation to live under `web/` and mandated the dedicated documentation/governance identifier format. The authoritative request mapping, scope, verification, and closure audit are in the [M0 Td S3 P1 main record](M0-Td-S3-P1-web-documentation-scope.md).

The task deliberately confines itself to `web/**` so its future commit is valid for `web/main`.
